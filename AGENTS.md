# AGENTS.md

本文件指导 AI 在本仓库中工作。项目实现面向 Windows/Linux、嵌入式 Linux 和二进制协议场景的 C++17 字节流编解码库。

## 开始工作前

- 阅读并遵守 [CONTRIBUTING.md](CONTRIBUTING.md) 中的编码、测试、性能验证和提交要求。
- 涉及 API、buffer、错误处理或自定义类型时，阅读 [DESIGN.md](DESIGN.md) 中的行为契约。
- 涉及编码格式时，阅读 [WIRE_FORMAT.md](WIRE_FORMAT.md)；基本用法和示例见 [README.md](README.md)。
- 详细规则在上述文档中维护，本文件只保留核心约束和 AI 工作要求。

## 核心约束

设计优先级依次为运行时性能、二进制体积和最小依赖、API 清晰度、安全性和正确性、向后兼容性。
性能优先不允许依赖未定义行为或省略必要的边界与溢出检查。

- 保持 C++17、GCC 9.x 和常见嵌入式交叉编译工具链可用；不兼容变更必须明确讨论并接受。
- 核心库保持无第三方运行时依赖。任何依赖提案须按贡献指南说明理由和影响。
- 优先编译期分发和可内联的小函数，避免隐藏分配、虚函数、类型擦除及不必要的抽象。
- 保持已有 API 和 wire format 契约；不要混入无关重命名、格式化或清理。

## 工作与验证

- 修改前识别受影响的契约和热路径，性能敏感改动先采集基线。
- 按贡献指南完成适用的构建、测试和 benchmark 对比；新增热路径功能应补充对应 benchmark。
- 不要因为测试失败而删除测试，也不要在没有 benchmark 数据时声称实现更快。
- API 和行为说明更新到 README、DESIGN；字节级变化更新到 WIRE_FORMAT 和黄金向量；开发流程更新到 CONTRIBUTING。
- 只有适用的验证和文档更新完成后才能宣称任务完成；无法验证的项目必须明确列出。

## 最终回复要求

每次完成修改后，回复应包含：

- 修改了哪些文件。
- 主要改动是什么。
- 是否影响 public API。
- 是否影响 wire format。
- 单元测试结果。
- benchmark 结果。
- 是否存在性能回退。
- 如有取舍，说明原因。
- 已知风险或后续建议。

如果没有实际运行测试或 benchmark，必须明确说明“未运行”，不能假装已经运行。
